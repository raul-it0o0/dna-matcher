#include <iostream>
#include <vector>

/**
 * Returns a vector of \b count randomly-chosen, non-overlapping substrings of \b string.
 */
std::vector<std::string> getNSubstrings(const std::string& string, int count);